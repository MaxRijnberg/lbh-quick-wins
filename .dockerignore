.git
.gitignore
.venv
__pycache__/
*.py[oc]
.pytest_cache/
.ipynb_checkpoints/
data/
tests/
Dockerfile
docker-compose.yml
.dockerignore

# Never bake secrets or the unit config (real email addresses) into the image.
# Both are supplied at runtime via docker-compose volume mounts instead.
.streamlit/secrets.toml
src/quick_wins/config/crowdstrike_config.json
