services:
  quick-wins:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./.streamlit/secrets.toml:/app/.streamlit/secrets.toml:ro
      - ./src/quick_wins/config/crowdstrike_config.json:/app/src/quick_wins/config/crowdstrike_config.json:ro
    restart: unless-stopped
